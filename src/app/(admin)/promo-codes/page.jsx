import PromoCodeClient from "./PromoCodeClient";

export const metadata = {
    title: "Promo Kodları",
    description: "Promo kodlarını yönetin.",
};

export default function PromoCodePage() {
    return <PromoCodeClient />;
}
