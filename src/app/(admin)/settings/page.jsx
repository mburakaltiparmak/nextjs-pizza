import SettingsClient from "./SettingsClient";

export const metadata = {
    title: "Admin Ayarları | Pizza App",
    description: "Yönetim paneli ayarları",
};

export default function SettingsPage() {
    return <SettingsClient />;
}
